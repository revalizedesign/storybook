import { Meta, Unstyled } from '@storybook/addon-docs/blocks'

<Meta title="Introduction" />

# Revalize design system storybook

Component library built on [shadcn/ui](https://ui.shadcn.com) with the base-vega preset, Base UI primitives, Tailwind CSS 4, and Inter font.

## Story nav groups

<Unstyled>
  <table className="w-full text-sm border-collapse">
    <thead>
      <tr className="border-b">
        <th className="text-left py-2 pr-4 font-semibold">Section</th>
        <th className="text-left py-2 font-semibold">Description</th>
      </tr>
    </thead>
    <tbody>
      <tr className="border-b"><td className="py-2 pr-4">Foundations</td><td className="py-2">Colors, icons, spacing, typography</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">shadcn</td><td className="py-2">All 56 standard shadcn components with live examples and doc links</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">Other frameworks</td><td className="py-2">Non-shadcn components from Radix Themes (Checkbox cards, Radio cards, Data list)</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">Originals</td><td className="py-2">Components we built that do not exist in shadcn — Agent status, Chat components, Autosave, etc.</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">Libraries</td><td className="py-2">Third-party components evaluated for adoption — Data table, Tree view</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">Layout</td><td className="py-2">App shell, band, slots, and composed layout examples</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">Multi-component UX</td><td className="py-2">Patterns that compose multiple components for a specific behavior — Search</td></tr>
      <tr className="border-b"><td className="py-2 pr-4">Patterns</td><td className="py-2">Structural compositions — App shell, AI admin</td></tr>
      <tr><td className="py-2 pr-4">Products</td><td className="py-2">Product-specific screens — Configure One, AutoQuotes, FlowIQ, SpecPage, Attainia</td></tr>
    </tbody>
  </table>
</Unstyled>
