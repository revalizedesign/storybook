import { Meta, Unstyled } from '@storybook/addon-docs/blocks'
import { StatusTable } from './StatusTable'

<Meta title="Status" />

# Status

Progress tracker mirroring the sidebar nav tree. Edit data in `src/stories/StatusTable.jsx` — `meta` for per-item values, `groupDefaults` for group baselines.

<Unstyled>
  <StatusTable />
</Unstyled>
