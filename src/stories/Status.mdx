import { Meta, Unstyled } from '@storybook/addon-docs/blocks'
import { StatusTable } from './StatusTable'

<Meta title="Status" />

# Status

Progress tracker mirroring the sidebar nav tree. Edit data in `src/stories/status.json` — set an item's values in `meta`; anything unset defaults to 0 hrs and Unknown status.

<Unstyled>
  <StatusTable />
</Unstyled>
